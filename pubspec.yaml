name: read_out_loud
description: A project that helps kids to read simple words

publish_to: 'none' 
version: 0.1.0

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  device_preview: ^1.1.0
  go_router: ^5.2.0
  flutter_riverpod: ^2.1.1
  speech_to_text: ^6.1.0
  dotted_border: ^2.0.0+3
  flutter_tts: ^3.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/wordlist1.json
